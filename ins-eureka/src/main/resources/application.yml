server:
 port: 8761
spring:
  application:
    name: ins-eureka

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url: http://localhost:8761/eureka/